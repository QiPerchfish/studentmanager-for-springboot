spring:
  profiles:
    active: dev
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
server:
  port: 8080
  compression:
    enabled: true
    mime-types: application/json
    min-response-size: 2048
logging:
  level:
    com.example.demo: DEBUG